import React from 'react';
import './Header.scss';

const Header = () => {
  return (
    <div className="app-header">
      <span className="title">Posts</span>
    </div>
  );
};

export default Header;
